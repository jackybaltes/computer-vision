{
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "next build && next export && echo \"\" >  out/.nojekyll && git add out/ && git commit -m \"deploy Next.js to gh-pages\" && git subtree push --prefix out github gh-pages"
  },
  "dependencies": {
    "@next/mdx": "^11.1.2",
    "next": "latest",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@mdx-js/loader": "^2.0.0",
    "babel-plugin-transform-define": "^1.3.0",
    "eslint": "8.9.0",
    "eslint-config-next": "12.0.10"
  }
}
